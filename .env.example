DATABASE_URL=postgresql://user:password@localhost:5432/dbname?sslmode=disable
TUTOR_ROOT=/absolute/path/to/Personal AI assistant
PYTHON_PATH=/usr/bin/python3
PORT=3000

# Comma-separated list of frontend origins allowed to call this API.
CORS_ORIGINS=http://localhost:4200

# Optional shared passcode. When set, every /api request must send a matching
# X-Access-Code header. Use this on public deployments to stop random traffic
# from burning your Anthropic/Gemini quota.
ACCESS_CODE=

# Gmail account used to send "Request a Demo" form submissions.
# GMAIL_APP_PASSWORD is a 16-character App Password from
# https://myaccount.google.com/apppasswords (requires 2-Step Verification),
# NOT your regular Gmail password.
GMAIL_USER=
GMAIL_APP_PASSWORD=
# Where demo requests are sent. Defaults to aeman098.fatima@gmail.com if unset.
DEMO_REQUEST_TO=
